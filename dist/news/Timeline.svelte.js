import './Timeline.svelte.css.proxy.js';
/* src/news/Timeline.svelte generated by Svelte v3.38.3 */
import {
	SvelteComponent,
	append,
	attr,
	binding_callbacks,
	check_outros,
	create_component,
	destroy_component,
	destroy_each,
	detach,
	element,
	empty,
	group_outros,
	init,
	insert,
	mount_component,
	noop,
	safe_not_equal,
	set_data,
	space,
	text,
	transition_in,
	transition_out
} from "../../_snowpack/pkg/svelte/internal.js";

import Button from "../shared/Button.svelte.js";
import Card from "../shared/Card.svelte.js";
import WebGL from "../shared/WebGL.svelte.js";
import Timestamp from "../shared/Timestamp.svelte.js";
import gradient from "../shaders/gradient.js";
import { onMount } from "../../_snowpack/pkg/svelte.js";
import { createClient } from "../../_snowpack/pkg/@supabase/supabase-js.js";

function get_each_context(ctx, list, i) {
	const child_ctx = ctx.slice();
	child_ctx[9] = list[i];
	return child_ctx;
}

// (104:8) {:else}
function create_else_block(ctx) {
	let t;
	let current_block_type_index;
	let if_block;
	let if_block_anchor;
	let current;
	let each_value = /*post_array*/ ctx[1];
	let each_blocks = [];

	for (let i = 0; i < each_value.length; i += 1) {
		each_blocks[i] = create_each_block(get_each_context(ctx, each_value, i));
	}

	const out = i => transition_out(each_blocks[i], 1, 1, () => {
		each_blocks[i] = null;
	});

	const if_block_creators = [create_if_block_1, create_else_block_1];
	const if_blocks = [];

	function select_block_type_1(ctx, dirty) {
		if (/*end*/ ctx[3]) return 0;
		return 1;
	}

	current_block_type_index = select_block_type_1(ctx, -1);
	if_block = if_blocks[current_block_type_index] = if_block_creators[current_block_type_index](ctx);

	return {
		c() {
			for (let i = 0; i < each_blocks.length; i += 1) {
				each_blocks[i].c();
			}

			t = space();
			if_block.c();
			if_block_anchor = empty();
		},
		m(target, anchor) {
			for (let i = 0; i < each_blocks.length; i += 1) {
				each_blocks[i].m(target, anchor);
			}

			insert(target, t, anchor);
			if_blocks[current_block_type_index].m(target, anchor);
			insert(target, if_block_anchor, anchor);
			current = true;
		},
		p(ctx, dirty) {
			if (dirty & /*post_array, Date*/ 2) {
				each_value = /*post_array*/ ctx[1];
				let i;

				for (i = 0; i < each_value.length; i += 1) {
					const child_ctx = get_each_context(ctx, each_value, i);

					if (each_blocks[i]) {
						each_blocks[i].p(child_ctx, dirty);
						transition_in(each_blocks[i], 1);
					} else {
						each_blocks[i] = create_each_block(child_ctx);
						each_blocks[i].c();
						transition_in(each_blocks[i], 1);
						each_blocks[i].m(t.parentNode, t);
					}
				}

				group_outros();

				for (i = each_value.length; i < each_blocks.length; i += 1) {
					out(i);
				}

				check_outros();
			}

			let previous_block_index = current_block_type_index;
			current_block_type_index = select_block_type_1(ctx, dirty);

			if (current_block_type_index === previous_block_index) {
				if_blocks[current_block_type_index].p(ctx, dirty);
			} else {
				group_outros();

				transition_out(if_blocks[previous_block_index], 1, 1, () => {
					if_blocks[previous_block_index] = null;
				});

				check_outros();
				if_block = if_blocks[current_block_type_index];

				if (!if_block) {
					if_block = if_blocks[current_block_type_index] = if_block_creators[current_block_type_index](ctx);
					if_block.c();
				} else {
					if_block.p(ctx, dirty);
				}

				transition_in(if_block, 1);
				if_block.m(if_block_anchor.parentNode, if_block_anchor);
			}
		},
		i(local) {
			if (current) return;

			for (let i = 0; i < each_value.length; i += 1) {
				transition_in(each_blocks[i]);
			}

			transition_in(if_block);
			current = true;
		},
		o(local) {
			each_blocks = each_blocks.filter(Boolean);

			for (let i = 0; i < each_blocks.length; i += 1) {
				transition_out(each_blocks[i]);
			}

			transition_out(if_block);
			current = false;
		},
		d(detaching) {
			destroy_each(each_blocks, detaching);
			if (detaching) detach(t);
			if_blocks[current_block_type_index].d(detaching);
			if (detaching) detach(if_block_anchor);
		}
	};
}

// (98:8) {#if status != ""}
function create_if_block(ctx) {
	let div;
	let card;
	let current;

	card = new Card({
			props: {
				size: "xl",
				$$slots: { content: [create_content_slot] },
				$$scope: { ctx }
			}
		});

	return {
		c() {
			div = element("div");
			create_component(card.$$.fragment);
			attr(div, "class", "col-span-3");
		},
		m(target, anchor) {
			insert(target, div, anchor);
			mount_component(card, div, null);
			current = true;
		},
		p(ctx, dirty) {
			const card_changes = {};

			if (dirty & /*$$scope, status*/ 4100) {
				card_changes.$$scope = { dirty, ctx };
			}

			card.$set(card_changes);
		},
		i(local) {
			if (current) return;
			transition_in(card.$$.fragment, local);
			current = true;
		},
		o(local) {
			transition_out(card.$$.fragment, local);
			current = false;
		},
		d(detaching) {
			if (detaching) detach(div);
			destroy_component(card);
		}
	};
}

// (108:24) 
function create_title_slot(ctx) {
	let h2;
	let t_value = /*post*/ ctx[9].title + "";
	let t;

	return {
		c() {
			h2 = element("h2");
			t = text(t_value);
			attr(h2, "slot", "title");
		},
		m(target, anchor) {
			insert(target, h2, anchor);
			append(h2, t);
		},
		p(ctx, dirty) {
			if (dirty & /*post_array*/ 2 && t_value !== (t_value = /*post*/ ctx[9].title + "")) set_data(t, t_value);
		},
		d(detaching) {
			if (detaching) detach(h2);
		}
	};
}

// (113:28) {#if post.link}
function create_if_block_2(ctx) {
	let button;
	let current;

	button = new Button({
			props: {
				href: /*post*/ ctx[9].link,
				$$slots: { default: [create_default_slot_1] },
				$$scope: { ctx }
			}
		});

	return {
		c() {
			create_component(button.$$.fragment);
		},
		m(target, anchor) {
			mount_component(button, target, anchor);
			current = true;
		},
		p(ctx, dirty) {
			const button_changes = {};
			if (dirty & /*post_array*/ 2) button_changes.href = /*post*/ ctx[9].link;

			if (dirty & /*$$scope, post_array*/ 4098) {
				button_changes.$$scope = { dirty, ctx };
			}

			button.$set(button_changes);
		},
		i(local) {
			if (current) return;
			transition_in(button.$$.fragment, local);
			current = true;
		},
		o(local) {
			transition_out(button.$$.fragment, local);
			current = false;
		},
		d(detaching) {
			destroy_component(button, detaching);
		}
	};
}

// (114:32) <Button href={post.link}>
function create_default_slot_1(ctx) {
	let t_value = (/*post*/ ctx[9].action ?? "Open") + "";
	let t;

	return {
		c() {
			t = text(t_value);
		},
		m(target, anchor) {
			insert(target, t, anchor);
		},
		p(ctx, dirty) {
			if (dirty & /*post_array*/ 2 && t_value !== (t_value = (/*post*/ ctx[9].action ?? "Open") + "")) set_data(t, t_value);
		},
		d(detaching) {
			if (detaching) detach(t);
		}
	};
}

// (109:24) 
function create_content_slot_1(ctx) {
	let div;
	let timestamp;
	let t0;
	let img;
	let img_src_value;
	let img_alt_value;
	let t1;
	let p;
	let t2_value = /*post*/ ctx[9].content + "";
	let t2;
	let t3;
	let current;

	timestamp = new Timestamp({
			props: {
				time: new Date(/*post*/ ctx[9].created_at)
			}
		});

	let if_block = /*post*/ ctx[9].link && create_if_block_2(ctx);

	return {
		c() {
			div = element("div");
			create_component(timestamp.$$.fragment);
			t0 = space();
			img = element("img");
			t1 = space();
			p = element("p");
			t2 = text(t2_value);
			t3 = space();
			if (if_block) if_block.c();
			attr(img, "class", "thumb");
			if (img.src !== (img_src_value = "https://chailagpncxzrnujqznl.supabase.in/storage/v1/object/public/post-images/" + /*post*/ ctx[9].image)) attr(img, "src", img_src_value);
			attr(img, "alt", img_alt_value = /*post*/ ctx[9].alt);
			attr(div, "class", "content");
			attr(div, "slot", "content");
		},
		m(target, anchor) {
			insert(target, div, anchor);
			mount_component(timestamp, div, null);
			append(div, t0);
			append(div, img);
			append(div, t1);
			append(div, p);
			append(p, t2);
			append(div, t3);
			if (if_block) if_block.m(div, null);
			current = true;
		},
		p(ctx, dirty) {
			const timestamp_changes = {};
			if (dirty & /*post_array*/ 2) timestamp_changes.time = new Date(/*post*/ ctx[9].created_at);
			timestamp.$set(timestamp_changes);

			if (!current || dirty & /*post_array*/ 2 && img.src !== (img_src_value = "https://chailagpncxzrnujqznl.supabase.in/storage/v1/object/public/post-images/" + /*post*/ ctx[9].image)) {
				attr(img, "src", img_src_value);
			}

			if (!current || dirty & /*post_array*/ 2 && img_alt_value !== (img_alt_value = /*post*/ ctx[9].alt)) {
				attr(img, "alt", img_alt_value);
			}

			if ((!current || dirty & /*post_array*/ 2) && t2_value !== (t2_value = /*post*/ ctx[9].content + "")) set_data(t2, t2_value);

			if (/*post*/ ctx[9].link) {
				if (if_block) {
					if_block.p(ctx, dirty);

					if (dirty & /*post_array*/ 2) {
						transition_in(if_block, 1);
					}
				} else {
					if_block = create_if_block_2(ctx);
					if_block.c();
					transition_in(if_block, 1);
					if_block.m(div, null);
				}
			} else if (if_block) {
				group_outros();

				transition_out(if_block, 1, 1, () => {
					if_block = null;
				});

				check_outros();
			}
		},
		i(local) {
			if (current) return;
			transition_in(timestamp.$$.fragment, local);
			transition_in(if_block);
			current = true;
		},
		o(local) {
			transition_out(timestamp.$$.fragment, local);
			transition_out(if_block);
			current = false;
		},
		d(detaching) {
			if (detaching) detach(div);
			destroy_component(timestamp);
			if (if_block) if_block.d();
		}
	};
}

// (105:12) {#each post_array as post}
function create_each_block(ctx) {
	let div;
	let card;
	let current;

	card = new Card({
			props: {
				size: "lg",
				$$slots: {
					content: [create_content_slot_1],
					title: [create_title_slot]
				},
				$$scope: { ctx }
			}
		});

	return {
		c() {
			div = element("div");
			create_component(card.$$.fragment);
			attr(div, "class", "mobile-first");
		},
		m(target, anchor) {
			insert(target, div, anchor);
			mount_component(card, div, null);
			current = true;
		},
		p(ctx, dirty) {
			const card_changes = {};

			if (dirty & /*$$scope, post_array*/ 4098) {
				card_changes.$$scope = { dirty, ctx };
			}

			card.$set(card_changes);
		},
		i(local) {
			if (current) return;
			transition_in(card.$$.fragment, local);
			current = true;
		},
		o(local) {
			transition_out(card.$$.fragment, local);
			current = false;
		},
		d(detaching) {
			if (detaching) detach(div);
			destroy_component(card);
		}
	};
}

// (124:12) {:else}
function create_else_block_1(ctx) {
	let div;
	let button;
	let current;

	button = new Button({
			props: {
				callback: /*nextPage*/ ctx[4],
				color: "green",
				$$slots: { default: [create_default_slot] },
				$$scope: { ctx }
			}
		});

	return {
		c() {
			div = element("div");
			create_component(button.$$.fragment);
			attr(div, "class", "col-span-3");
		},
		m(target, anchor) {
			insert(target, div, anchor);
			mount_component(button, div, null);
			current = true;
		},
		p(ctx, dirty) {
			const button_changes = {};

			if (dirty & /*$$scope*/ 4096) {
				button_changes.$$scope = { dirty, ctx };
			}

			button.$set(button_changes);
		},
		i(local) {
			if (current) return;
			transition_in(button.$$.fragment, local);
			current = true;
		},
		o(local) {
			transition_out(button.$$.fragment, local);
			current = false;
		},
		d(detaching) {
			if (detaching) detach(div);
			destroy_component(button);
		}
	};
}

// (122:12) {#if end}
function create_if_block_1(ctx) {
	let p;

	return {
		c() {
			p = element("p");
			p.textContent = "That's all...";
			attr(p, "class", "more svelte-pgp7nd");
		},
		m(target, anchor) {
			insert(target, p, anchor);
		},
		p: noop,
		i: noop,
		o: noop,
		d(detaching) {
			if (detaching) detach(p);
		}
	};
}

// (126:20) <Button callback={nextPage} color="green">
function create_default_slot(ctx) {
	let t;

	return {
		c() {
			t = text("Load more");
		},
		m(target, anchor) {
			insert(target, t, anchor);
		},
		d(detaching) {
			if (detaching) detach(t);
		}
	};
}

// (101:16) 
function create_content_slot(ctx) {
	let p;
	let t;

	return {
		c() {
			p = element("p");
			t = text(/*status*/ ctx[2]);
			attr(p, "class", "content");
			attr(p, "slot", "content");
		},
		m(target, anchor) {
			insert(target, p, anchor);
			append(p, t);
		},
		p(ctx, dirty) {
			if (dirty & /*status*/ 4) set_data(t, /*status*/ ctx[2]);
		},
		d(detaching) {
			if (detaching) detach(p);
		}
	};
}

function create_fragment(ctx) {
	let div1;
	let div0;
	let current_block_type_index;
	let if_block;
	let t;
	let div2;
	let webgl;
	let current;
	const if_block_creators = [create_if_block, create_else_block];
	const if_blocks = [];

	function select_block_type(ctx, dirty) {
		if (/*status*/ ctx[2] != "") return 0;
		return 1;
	}

	current_block_type_index = select_block_type(ctx, -1);
	if_block = if_blocks[current_block_type_index] = if_block_creators[current_block_type_index](ctx);
	webgl = new WebGL({ props: { shader_import: gradient } });

	return {
		c() {
			div1 = element("div");
			div0 = element("div");
			if_block.c();
			t = space();
			div2 = element("div");
			create_component(webgl.$$.fragment);
			attr(div0, "class", "news-grid svelte-pgp7nd");
			attr(div1, "class", "Timeline svelte-pgp7nd");
			attr(div2, "class", "bg svelte-pgp7nd");
		},
		m(target, anchor) {
			insert(target, div1, anchor);
			append(div1, div0);
			if_blocks[current_block_type_index].m(div0, null);
			/*div1_binding*/ ctx[5](div1);
			insert(target, t, anchor);
			insert(target, div2, anchor);
			mount_component(webgl, div2, null);
			current = true;
		},
		p(ctx, [dirty]) {
			let previous_block_index = current_block_type_index;
			current_block_type_index = select_block_type(ctx, dirty);

			if (current_block_type_index === previous_block_index) {
				if_blocks[current_block_type_index].p(ctx, dirty);
			} else {
				group_outros();

				transition_out(if_blocks[previous_block_index], 1, 1, () => {
					if_blocks[previous_block_index] = null;
				});

				check_outros();
				if_block = if_blocks[current_block_type_index];

				if (!if_block) {
					if_block = if_blocks[current_block_type_index] = if_block_creators[current_block_type_index](ctx);
					if_block.c();
				} else {
					if_block.p(ctx, dirty);
				}

				transition_in(if_block, 1);
				if_block.m(div0, null);
			}
		},
		i(local) {
			if (current) return;
			transition_in(if_block);
			transition_in(webgl.$$.fragment, local);
			current = true;
		},
		o(local) {
			transition_out(if_block);
			transition_out(webgl.$$.fragment, local);
			current = false;
		},
		d(detaching) {
			if (detaching) detach(div1);
			if_blocks[current_block_type_index].d();
			/*div1_binding*/ ctx[5](null);
			if (detaching) detach(t);
			if (detaching) detach(div2);
			destroy_component(webgl);
		}
	};
}

const PER_PAGE = 9;
const supabaseUrl = "https://chailagpncxzrnujqznl.supabase.co";
const supabaseKey = "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJyb2xlIjoiYW5vbiIsImlhdCI6MTYzNzk0NDE2MywiZXhwIjoxOTUzNTIwMTYzfQ.j7HbrIrT1elnirf8zk1hT3cM0xi6JLc9A92qH4_rEJ0";

function instance($$self, $$props, $$invalidate) {
	let timeline;
	let post_array = [];
	let current_page = 0;
	let status = "Getting posts...";
	let end = false;
	const supabase = createClient(supabaseUrl, supabaseKey);

	async function populate(page) {
		let { data: posts, error } = await supabase.from("posts").select("*").order("created_at", { ascending: false }).range(page * PER_PAGE, (page + 1) * PER_PAGE - 1);

		if (error) {
			$$invalidate(2, status = "Error while getting posts.");
		} else if (posts.length == 0) {
			$$invalidate(3, end = true);

			if (post_array.length == 0) {
				$$invalidate(2, status = "No posts found.");
			}
		} else {
			if (posts.length < PER_PAGE) {
				$$invalidate(3, end = true);
			}

			$$invalidate(2, status = "");
			$$invalidate(1, post_array = post_array.concat(posts));
		}
	}

	async function nextPage() {
		current_page += 1;
		await populate(current_page);
		console.log(current_page);
	}

	onMount(async () => {
		await populate(current_page);
	});

	function div1_binding($$value) {
		binding_callbacks[$$value ? "unshift" : "push"](() => {
			timeline = $$value;
			$$invalidate(0, timeline);
		});
	}

	return [timeline, post_array, status, end, nextPage, div1_binding];
}

class Timeline extends SvelteComponent {
	constructor(options) {
		super();
		init(this, options, instance, create_fragment, safe_not_equal, {});
	}
}

export default Timeline;