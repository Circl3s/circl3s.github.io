import './Navbar.svelte.css.proxy.js';
/* src/shared/Navbar.svelte generated by Svelte v3.38.3 */
import {
	SvelteComponent,
	add_render_callback,
	append,
	attr,
	detach,
	element,
	globals,
	init,
	insert,
	listen,
	noop,
	safe_not_equal,
	space
} from "../../_snowpack/pkg/svelte/internal.js";

const { window: window_1 } = globals;

function create_fragment(ctx) {
	let scrolling = false;

	let clear_scrolling = () => {
		scrolling = false;
	};

	let scrolling_timeout;
	let div1;
	let h2;
	let t1;
	let div0;
	let div1_class_value;
	let mounted;
	let dispose;
	add_render_callback(/*onwindowscroll*/ ctx[1]);

	return {
		c() {
			div1 = element("div");
			h2 = element("h2");
			h2.textContent = "Circl3s";
			t1 = space();
			div0 = element("div");

			div0.innerHTML = `<a href="#about" class="svelte-4cmdoz">About</a> 
        <a href="#hobbies" class="svelte-4cmdoz">Hobbies</a> 
        <a href="#toolbox" class="svelte-4cmdoz">Toolbox</a> 
        <a href="#social" class="svelte-4cmdoz">Social</a>`;

			attr(h2, "class", "svelte-4cmdoz");
			attr(div0, "class", "links");
			attr(div1, "class", div1_class_value = "Navbar " + (/*scroll*/ ctx[0] == 0 ? "" : "bg") + " svelte-4cmdoz");
		},
		m(target, anchor) {
			insert(target, div1, anchor);
			append(div1, h2);
			append(div1, t1);
			append(div1, div0);

			if (!mounted) {
				dispose = listen(window_1, "scroll", () => {
					scrolling = true;
					clearTimeout(scrolling_timeout);
					scrolling_timeout = setTimeout(clear_scrolling, 100);
					/*onwindowscroll*/ ctx[1]();
				});

				mounted = true;
			}
		},
		p(ctx, [dirty]) {
			if (dirty & /*scroll*/ 1 && !scrolling) {
				scrolling = true;
				clearTimeout(scrolling_timeout);
				scrollTo(window_1.pageXOffset, /*scroll*/ ctx[0]);
				scrolling_timeout = setTimeout(clear_scrolling, 100);
			}

			if (dirty & /*scroll*/ 1 && div1_class_value !== (div1_class_value = "Navbar " + (/*scroll*/ ctx[0] == 0 ? "" : "bg") + " svelte-4cmdoz")) {
				attr(div1, "class", div1_class_value);
			}
		},
		i: noop,
		o: noop,
		d(detaching) {
			if (detaching) detach(div1);
			mounted = false;
			dispose();
		}
	};
}

function instance($$self, $$props, $$invalidate) {
	let scroll = 0;
	console.log(scroll);

	function handleScroll(e) {
		$$invalidate(0, scroll = window.scrollY);
		console.log(scroll);
	}

	function onwindowscroll() {
		$$invalidate(0, scroll = window_1.pageYOffset)
	}

	return [scroll, onwindowscroll];
}

class Navbar extends SvelteComponent {
	constructor(options) {
		super();
		init(this, options, instance, create_fragment, safe_not_equal, {});
	}
}

export default Navbar;