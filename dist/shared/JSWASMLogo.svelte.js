import './JSWASMLogo.svelte.css.proxy.js';
/* src/shared/JSWASMLogo.svelte generated by Svelte v3.38.3 */
import {
	SvelteComponent,
	append,
	attr,
	detach,
	element,
	globals,
	init,
	insert,
	listen,
	noop,
	safe_not_equal,
	space
} from "../../_snowpack/pkg/svelte/internal.js";

const { window: window_1 } = globals;

function create_else_block(ctx) {
	let img;
	let img_src_value;

	return {
		c() {
			img = element("img");
			if (img.src !== (img_src_value = "img/WebAssembly_Horizontal.svg")) attr(img, "src", img_src_value);
			attr(img, "alt", "WebAssembly Logo");
			attr(img, "class", "svelte-j7fy03");
		},
		m(target, anchor) {
			insert(target, img, anchor);
		},
		d(detaching) {
			if (detaching) detach(img);
		}
	};
}

// (37:4) {#if wide}
function create_if_block(ctx) {
	let img;
	let img_src_value;

	return {
		c() {
			img = element("img");
			if (img.src !== (img_src_value = "img/WebAssembly.svg")) attr(img, "src", img_src_value);
			attr(img, "alt", "WebAssembly Logo");
			attr(img, "class", "svelte-j7fy03");
		},
		m(target, anchor) {
			insert(target, img, anchor);
		},
		d(detaching) {
			if (detaching) detach(img);
		}
	};
}

function create_fragment(ctx) {
	let div;
	let img;
	let img_src_value;
	let t;
	let mounted;
	let dispose;

	function select_block_type(ctx, dirty) {
		if (/*wide*/ ctx[0]) return create_if_block;
		return create_else_block;
	}

	let current_block_type = select_block_type(ctx, -1);
	let if_block = current_block_type(ctx);

	return {
		c() {
			div = element("div");
			img = element("img");
			t = space();
			if_block.c();
			if (img.src !== (img_src_value = "img/JavaScript.svg")) attr(img, "src", img_src_value);
			attr(img, "alt", "JavaScript Logo");
			attr(img, "class", "svelte-j7fy03");
			attr(div, "class", "Logo svelte-j7fy03");
		},
		m(target, anchor) {
			insert(target, div, anchor);
			append(div, img);
			append(div, t);
			if_block.m(div, null);

			if (!mounted) {
				dispose = listen(window_1, "resize", /*handleResize*/ ctx[1]);
				mounted = true;
			}
		},
		p(ctx, [dirty]) {
			if (current_block_type !== (current_block_type = select_block_type(ctx, dirty))) {
				if_block.d(1);
				if_block = current_block_type(ctx);

				if (if_block) {
					if_block.c();
					if_block.m(div, null);
				}
			}
		},
		i: noop,
		o: noop,
		d(detaching) {
			if (detaching) detach(div);
			if_block.d();
			mounted = false;
			dispose();
		}
	};
}

function instance($$self, $$props, $$invalidate) {
	let wide;

	function handleResize(e) {
		$$invalidate(0, wide = window.innerWidth > 768);
	}

	$: $$invalidate(0, wide = window.innerWidth > 768);
	return [wide, handleResize];
}

class JSWASMLogo extends SvelteComponent {
	constructor(options) {
		super();
		init(this, options, instance, create_fragment, safe_not_equal, {});
	}
}

export default JSWASMLogo;